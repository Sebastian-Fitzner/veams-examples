{{! ---
general: u-segment
============================================

Requirements:
- (only hbs)

Options:
- segmentClasses {String} - Modifier classes

Important Notes:

--- }}


<div class="u-segment{{#if options.segmentClasses}} {{options.segmentClasses}}{{/if}}">
	{{#if options.headline}}
		<div class="segment__header">
			<h2 class="segment__headline">{{options.headline}}</h2>
		</div>
	{{/if}}

	<div class="segment__content{{#if options.centered}} is-centered{{/if}}">
		{{{yield}}}
	</div>
</div>
